import { YellowDirective } from './yellow.directive';

describe('YellowDirective', () => {
  it('should create an instance', () => {
    const directive = new YellowDirective();
    expect(directive).toBeTruthy();
  });
});
